Teclado Linux--> sudo setxkbmap -layout 'es,es' -model pc105

Una vez instalas GIT, debes configurarlo

git config --global user.name "Christian" git config --global user.email "xxxxxx@xxxx.com"

Generando tu Public Key:
ssh-keygen

Leyendo tu llave para copiarla a Github:
cat ~/.ssh/id_rsa.pub
-----------------------------------------------
git init (crear repo)
git add nombre (agregar file al repo)
git commit -m "mensaje" (enviar archivo con msj)
git remote add origin git@github.com:yorvic/programacion1.git (conectarse al repo)

git pull origin master (traer archivos)
git push origin master (enviar archivos)
